from __future__ import annotations


class LlmClientPlaceholder:
    """Reserved for BE-level answer generation after KB V1 is benchmarked."""

    def generate(self, prompt: str) -> str:
        return prompt
